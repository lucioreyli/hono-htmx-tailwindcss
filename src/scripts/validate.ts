// // declare const htmx;
// import * as v from "valibot";

// const form = htmx.find("#login-form") as HTMLFormElement;
// console.log(Array.from(htmx.values(form).entries()));
// // const schema = v.object()
// form.onsubmit = (e) => {};
// htmx.addClass(form, "border-2");
